---
title: Merge PR 59 G7 guard stack to main (Plan 086)
status: completed
created: 2026-05-28
---

# Plan 086 — Ship PR #59 to `main`

## Problem

PR #59 is **MERGEABLE** with green CI but `main` still ends at PR #57 (#75 bundle). The G7 false-neural guard stack (Plans 078–085) must land on `main` before the next admission gate work.

## Scope

- Assert hosted golden smoke workflow writes JSON with `--json` and uploads `hosted-golden-smoke.json` (workflow contract test).
- Merge **PR #59** (squash) into `main`.
- Record Plan 086 validation in `docs/knowledgebase/40-operational-risk/hosted-validation-2026-05-23.md` with merge commit SHA.
- Mark this plan `status: completed`.

## Out of scope

- Hunyuan adapter enablement or closing G7/G8/G9 gates.
- Claiming ZeroGPU neural validation (cpu-demo fallback remains).

## Test scenarios

1. `tests.test_workflows` includes hosted golden smoke `--json` + artifact path assertions.
2. After merge: `git log origin/main -1` contains G7 guard changes; `tests.test_hosted_golden_smoke` passes on `main`.
3. Live `hosted_golden_smoke.py --json` on Space: `g7_false_neural_guard_ok=true`, Local CPU Preview.

## Files

- `docs/plans/2026-05-28-086-ship-pr59-merge.md`
- `tests/test_workflows.py`
- `docs/knowledgebase/40-operational-risk/hosted-validation-2026-05-23.md`
