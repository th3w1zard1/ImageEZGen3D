---
title: Merge PR 60 smoke record verifier to main (Plan 088)
status: completed
created: 2026-05-28
---

# Plan 088 — Ship PR #60 to `main`

## Problem

PR #60 (Plan 087) adds `verify_hosted_golden_smoke_record.py` and a workflow gate. CI is green and the branch is **MERGEABLE**, but `main` still lacks the verifier.

## Scope

- Squash-merge **PR #60** into `main`.
- Record Plan 088 validation in `hosted-validation-2026-05-23.md` with merge SHA.
- On `main`: run unit tests, live smoke `--json`, and `verify_hosted_golden_smoke_record.py` on a recorded smoke file.
- Mark this plan `status: completed`.

## Out of scope

- Hunyuan enablement or closing G7/G8/G9 gates.
- HF Space redeploy (no `app.py` changes in PR #60).

## Test scenarios

1. After merge: `tests.test_hosted_golden_smoke` and `tests.test_workflows` pass on `main`.
2. `hosted_golden_smoke.py --record /tmp/smoke.json` then `verify_hosted_golden_smoke_record.py` exits 0.
3. Live smoke: `g7_false_neural_guard_ok=true`, Local CPU Preview (cpu-demo fallback).

## Files

- `docs/plans/2026-05-28-088-ship-pr60-merge.md`
- `docs/knowledgebase/40-operational-risk/hosted-validation-2026-05-23.md`
