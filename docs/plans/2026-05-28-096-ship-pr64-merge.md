---
title: Merge PR 64 G7 live probe to main (Plan 096)
status: completed
created: 2026-05-28
---

# Plan 096 — Ship PR #64 to `main`

## Problem

PR #64 (Plan 095) adds scheduled G7 `--live-probe` and `verify_hosted_smoke_artifacts.py`. CI must be green and branch **MERGEABLE** before merge.

## Scope

- Squash-merge **PR #64** into `main`.
- Record Plan 096 validation in `hosted-validation-2026-05-23.md` with merge SHA.
- On `main`: unit tests, live G7 `--live-probe --json`, golden smoke, verify smoke artifacts CLI.
- Mark this plan `status: completed`.

## Out of scope

- Hunyuan enablement or closing G7 gate.

## Test scenarios

1. `tests.test_hunyuan_g7_preflight` and `tests.test_hosted_smoke_artifacts` pass on `main`.
2. Live G7 probe: `ok=true`, probe rejects disabled adapter (not false neural success).
3. Golden smoke: `g7_false_neural_guard_ok=true`, Local CPU Preview.

## Files

- `docs/plans/2026-05-28-096-ship-pr64-merge.md`
- `docs/knowledgebase/40-operational-risk/hosted-validation-2026-05-23.md`
